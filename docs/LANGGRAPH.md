# LangGraph chat (planned — Wave 1/2)

Graph: classify_intent → retrieve_chunks → analyze → generate → cite

LLM: self-hosted `Qwen2.5-7B-Instruct` via Ollama/vLLM — no external API.

Tools: search_documents, get_extraction, list_project_documents
