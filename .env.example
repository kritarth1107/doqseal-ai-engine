# Internal service — not exposed to frontend
MONGODB_URI=mongodb://localhost:27017/doqseal
AMQP_URI=amqp://doqseal:doqseal@localhost:5672
EXTRACTION_QUEUE=extraction.jobs
STORAGE_ROOT=../storage

# Must match backend/.env AES_SECRET exactly
AES_SECRET=your_aes_secret_key_here_min_32_chars

# Extraction pipeline
# hybrid = OCR + Qwen2.5-VL (falls back to OCR if VLM fails)
# ocr_only = PaddleOCR parsing only (no GPU model)
# stub = fake demo data (Phase 0 behaviour)
EXTRACTION_MODE=hybrid
VLM_MODEL=Qwen/Qwen2.5-VL-3B-Instruct
VLM_USE_4BIT=true
MAX_PDF_PAGES=3
OCR_LANGUAGES=en,hi
CONFIDENCE_THRESHOLD=0.75

# FastAPI health (internal)
HOST=0.0.0.0
PORT=3031